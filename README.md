# CourseRate - Платформа рейтинга обучающих программ

Независимая платформа для поиска, сравнения и оценки обучающих программ на основе отзывов реальных пользователей.

## Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк для создания API
- **PostgreSQL** - реляционная база данных
- **SQLAlchemy** - ORM для работы с БД
- **Alembic** - миграции БД
- **Redis** - кэширование
- **JWT** - аутентификация

### Frontend
- **React** - библиотека для создания UI
- **Vite** - сборщик проектов
- **TailwindCSS** - CSS фреймворк
- **React Router** - маршрутизация
- **Axios** - HTTP клиент

## Структура проекта

```
Obuchenie/
├── backend/           # Backend API (FastAPI)
│   ├── app/
│   │   ├── api/       # API endpoints
│   │   ├── core/      # Конфигурация и безопасность
│   │   ├── models/    # Модели БД
│   │   ├── schemas/   # Pydantic схемы
│   │   ├── services/  # Бизнес-логика
│   │   └── db/        # Настройки БД
│   └── requirements.txt
├── frontend/          # Frontend (React)
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   └── services/
│   └── package.json
├── docker-compose.yml
└── README.md
```

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd Obuchenie
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и настройте переменные:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл с вашими настройками.

### 3. Запуск с Docker Compose (рекомендуется)

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

Сервисы будут доступны:
- **Backend API**: http://localhost:8000
- **API Docs (Swagger)**: http://localhost:8000/docs
- **PostgreSQL**: localhost:5432
- **Redis**: localhost:6379

### 4. Запуск без Docker

#### Backend

```bash
cd backend

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
uvicorn app.main:app --reload
```

#### PostgreSQL (необходимо установить отдельно)

```bash
# Создание базы данных
createdb courserate

# Или через psql
psql -U postgres
CREATE DATABASE courserate;
```

#### Frontend (будет добавлен позже)

```bash
cd frontend
npm install
npm run dev
```

## Основные возможности

### Для всех пользователей
- Просмотр курсов и рейтингов
- Чтение отзывов
- Поиск и фильтрация курсов
- Сортировка по различным критериям

### Для зарегистрированных пользователей
- Регистрация и вход в систему
- Добавление новых курсов
- Написание отзывов
- Оценка курсов по 5 критериям
- Добавление курсов в избранное
- Редактирование своих отзывов

### Для администраторов
- Модерация курсов и отзывов
- Управление категориями
- Блокировка пользователей
- Управление премиум-размещением

## API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход
- `GET /api/v1/auth/me` - Текущий пользователь

### Курсы
- `GET /api/v1/courses` - Список курсов
- `GET /api/v1/courses/{id}` - Детали курса
- `POST /api/v1/courses` - Создать курс
- `PUT /api/v1/courses/{id}` - Обновить курс
- `DELETE /api/v1/courses/{id}` - Удалить курс

### Отзывы
- `GET /api/v1/reviews` - Список отзывов
- `POST /api/v1/reviews` - Создать отзыв
- `PUT /api/v1/reviews/{id}` - Обновить отзыв
- `DELETE /api/v1/reviews/{id}` - Удалить отзыв

### Категории
- `GET /api/v1/categories` - Список категорий
- `POST /api/v1/categories` - Создать категорию

Полная документация доступна по адресу: http://localhost:8000/docs

## База данных

### Модели

- **User** - Пользователи
- **Course** - Курсы
- **Review** - Отзывы
- **Category** - Категории
- **Subcategory** - Подкатегории
- **Tag** - Теги
- **Favorite** - Избранное
- **Report** - Жалобы
- **PremiumPlacement** - Премиум размещение

### Миграции

```bash
# Создание миграции
alembic revision --autogenerate -m "description"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1
```

## Система оценки

Курсы оцениваются по 5 критериям (каждый от 1 до 5):

1. **Качество материала** - насколько полезен и актуален контент
2. **Преподаватели** - профессионализм и манера подачи
3. **Поддержка** - как быстро отвечают, помогают ли
4. **Цена/качество** - стоит ли своих денег
5. **Практическая польза** - можно ли применить знания в работе

**Общий рейтинг** = среднее арифметическое всех критериев

## Антиспам меры

- Email-верификация обязательна
- Лимит: 3 отзыва в день
- Минимальный возраст аккаунта для отзыва - 3 дня
- Один отзыв на курс от одного пользователя
- Редактирование отзыва не чаще 1 раза в 30 дней

## Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- Защита от SQL-инъекций через SQLAlchemy
- Валидация данных через Pydantic
- CORS настроен для разрешенных доменов

## Разработка

### Запуск тестов

```bash
cd backend
pytest
```

### Линтинг

```bash
# Backend
cd backend
black .
flake8 .

# Frontend
cd frontend
npm run lint
```

## Монетизация

### Премиум размещение для курсов

1. **Базовое** (бесплатно) - стандартная карточка
2. **Выделенное** ($30/мес) - цветная рамка, выше в списке
3. **Топ** ($80/мес) - закрепление в топ-3, слайдер на главной

## Roadmap

### Фаза 1 (MVP) ✅
- [x] Backend API
- [x] Модели БД
- [x] Аутентификация
- [x] CRUD для курсов и отзывов
- [ ] Frontend базовый

### Фаза 2
- [ ] Премиум размещение
- [ ] Расширенная модерация
- [ ] Email уведомления
- [ ] Поиск с автодополнением
- [ ] SEO оптимизация

### Фаза 3
- [ ] Сравнение курсов
- [ ] Персональные рекомендации
- [ ] Telegram бот
- [ ] Мобильное приложение

## Лицензия

MIT

## Контакты

По вопросам и предложениям: [email]

---

**Сделано с ❤️ для образовательного комьюнити**
